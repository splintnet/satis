apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "satis.fullname" . }}-test-connection"
  labels:
    {{- include "satis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "satis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
